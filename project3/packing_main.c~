#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>

typedef struct node{
  int thisnode;
  int parnode;
  int lcnode;
  int rcnode;
  char cutline;
  int width;
  int height;
}Node;

int main(int argc, char * * argv)
{
  
   char *filename1 = argv[1];
   Node *arr = Load_File(filename1);
   
   
   return 0;
}